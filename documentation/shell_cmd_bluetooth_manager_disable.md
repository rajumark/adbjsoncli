# `adbjson shell cmd bluetooth_manager disable`

## adbjson

**Command:**
```bash
adbjson shell cmd bluetooth_manager disable
```

**Output:**
```json
{
  "status": 0,
  "output": "ShellCommand: Exec disable\ndisable: Success"
}
```

---

## adb

**Command:**
```bash
adb shell cmd bluetooth_manager disable
```

**Output:**
```
ShellCommand: Exec disable
disable: Success
```
