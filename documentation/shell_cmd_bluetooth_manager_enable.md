# `adbjson shell cmd bluetooth_manager enable`

## adbjson

**Command:**
```bash
adbjson shell cmd bluetooth_manager enable
```

**Output:**
```json
{
  "status": 0,
  "output": "ShellCommand: Exec enable\nenable: Success"
}
```

---

## adb

**Command:**
```bash
adb shell cmd bluetooth_manager enable
```

**Output:**
```
ShellCommand: Exec enable
enable: Success
```
